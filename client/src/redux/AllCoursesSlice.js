import { createSlice } from "@reduxjs/toolkit";

const initialState = [];

export const allCoursesSlice = createSlice({
    name: "allCoursesSlice",
    initialState,
    reducers: {
        
    }
})